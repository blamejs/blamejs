"use strict";

module.exports = {
  slug:  "guard-execution",
  title: "Guard Execution Strings",
  body: [
  "<h1>Guard Execution Strings</h1>",
  "<p>Execution-string guards defend values that become code-like input somewhere else: shell arguments, regex patterns, JSONPath selectors, and template source. This page contains the full documentation for the shell, regex, JSONPath, and template guards.</p>",
  "<ul>",
  "  <li><a href=\"#guard-shell\">Shell argument safety</a></li>",
  "  <li><a href=\"#guard-regex\">Regex pattern safety</a></li>",
  "  <li><a href=\"#guard-jsonpath\">JSONPath safety</a></li>",
  "  <li><a href=\"#guard-template\">Template-injection safety</a></li>",
  "</ul>",
  "<h2 id=\"guard-shell\">Shell argument safety <a class=\"anchor\" href=\"#guard-shell\">#</a></h2>",
  "<p><code>b.guardShell</code> validates user-input strings BEFORE they're handed to a child-process spawn (regardless of operator's <code>shell:</code> opt). The canonical defense is \"use array args + <code>shell:false</code>\", but operators still receive operator-untrusted strings flowing through path-arg or arg-list shapes — guardShell refuses obvious shell-injection shapes before the spawn call. KIND=\"identifier\" — consumes <code>ctx.identifier</code> (or <code>ctx.arg</code>). Threat catalog: POSIX + cmd.exe metacharacters, $(...) / ${...} command + parameter substitution, backtick substitution, process substitution, $VAR parameter expansion, newline injection, leading-hyphen option-flag injection, BIDI / null / control / zero-width universal refuse.</p>",
  "<h2 id=\"guard-shell-validate\">b.guardShell.validate(arg, opts) <a class=\"anchor\" href=\"#guard-shell-validate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:             \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:   \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  posixMetaPolicy:     \"reject\" | \"audit\" | \"allow\",",
  "  cmdMetaPolicy:       \"reject\" | \"audit\" | \"allow\",",
  "  dollarSubstPolicy:   \"reject\" | \"allow\",          // refused at every profile",
  "  processSubstPolicy:  \"reject\" | \"allow\",          // refused at every profile",
  "  backtickPolicy:      \"reject\" | \"allow\",          // refused at every profile",
  "  newlinePolicy:       \"reject\" | \"allow\",          // refused at every profile",
  "  argHyphenPolicy:     \"reject\" | \"audit\" | \"allow\",",
  "  bidiPolicy:          \"reject\" | \"allow\",          // refused at every profile",
  "  controlPolicy:       \"reject\" | \"allow\",          // refused at every profile",
  "  nullBytePolicy:      \"reject\" | \"allow\",          // refused at every profile",
  "  zeroWidthPolicy:     \"reject\" | \"allow\",          // refused at every profile",
  "  maxBytes:            number,                       // default 2 KiB (8 KiB permissive)",
  "}</code></pre>",
  "<p>Validates a single shell-argument string. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>shell-cap</code>, <code>posix-metachar</code>, <code>cmd-metachar</code>, <code>dollar-substitution</code>, <code>backtick</code>, <code>process-substitution</code>, <code>dollar-var</code>, <code>newline</code>, <code>arg-hyphen-leading</code>, plus the universal codepoint-class kinds.</p>",
  "<pre><code class=\"language-javascript\">b.guardShell.validate(\"safe-arg-value\", { profile: \"strict\" });",
  "// → { ok: true, issues: [] }",
  "",
  "// Command-injection chain.",
  "b.guardShell.validate(\"safe; rm -rf /\", { profile: \"strict\" });",
  "// → ok=false; posix-metachar + cmd-metachar",
  "",
  "// Command + parameter substitution — universal refuse.",
  "b.guardShell.validate(\"input$\" + \"(whoami)\",  { profile: \"strict\" });",
  "b.guardShell.validate(\"input$\" + \"{HOME}\",    { profile: \"strict\" });",
  "b.guardShell.validate(\"input`whoami`\",        { profile: \"strict\" });",
  "b.guardShell.validate(\"file<(cat /etc/pw)\",   { profile: \"strict\" });",
  "// → ok=false; (dollar-substitution | backtick | process-substitution)",
  "",
  "// Leading-hyphen option-flag injection.",
  "b.guardShell.validate(\"-rf\",         { profile: \"strict\" });",
  "b.guardShell.validate(\"--exec=cmd\",  { profile: \"strict\" });",
  "// → ok=false; arg-hyphen-leading",
  "</code></pre>",
  "<h2 id=\"guard-shell-sanitize\">b.guardShell.sanitize(arg, opts) <a class=\"anchor\" href=\"#guard-shell-sanitize\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:           \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture: \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "}</code></pre>",
  "<p>Shell args can't be repaired — <code>sanitize</code> either passes through valid input or throws.</p>",
  "<pre><code class=\"language-javascript\">var arg = \"user-supplied-filename.txt\";",
  "var safe = b.guardShell.sanitize(arg, { profile: \"strict\" });",
  "// safe === arg when validation passes; throws otherwise.</code></pre>",
  "<h2 id=\"guard-shell-gate\">b.guardShell.gate(opts) <a class=\"anchor\" href=\"#guard-shell-gate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:               \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:     \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  mode:                  \"enforce\" | \"warn-only\" | \"shadow\" | \"audit-only\" | \"log-only\" | \"canary\",",
  "  audit:                 b.audit,",
  "  observability:         b.observability,",
  "  forensicEvidenceStore: { put }, forensicSnippetBytes: number,",
  "  cache:                 b.cache, cacheTtlMs: number,",
  "  maxRuntimeMs:          number,",
  "}</code></pre>",
  "<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.arg</code>); empty / missing argument resolves to <code>{ ok: true, action: \"serve\" }</code>.</p>",
  "<pre><code class=\"language-javascript\">var shellGate = b.guardShell.gate({ profile: \"strict\", audit: b.audit });",
  "",
  "app.post(\"/admin/run-job\", async function (req, res) {",
  "  var rv = await shellGate.check({ identifier: req.body.target });",
  "  if (!rv.ok) return res.status(400).json({ refused: rv.issues });",
  "  // ... pass req.body.target to spawn (still use array args + shell:false) ...",
  "});</code></pre>",
  "<h2 id=\"guard-shell-threat-catalog\">Threat catalog <a class=\"anchor\" href=\"#guard-shell-threat-catalog\">#</a></h2>",
  "<ul>",
  "<li><strong>POSIX shell metacharacters</strong> — <code>;</code>, <code>&amp;</code>, <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>(</code>, <code>)</code>, <code>{</code>, <code>}</code>, <code>[</code>, <code>]</code>, <code>*</code>, <code>?</code>, <code>~</code>, <code>!</code>, <code>#</code>, <code>\\</code>, single + double quotes.</li>",
  "<li><strong>Backtick command substitution</strong> — <code>`whoami`</code> universally refused.</li>",
  "<li><strong>$(...) and ${VAR}</strong> — POSIX shell command + parameter substitution universally refused.</li>",
  "<li><strong>Process substitution</strong> — <code>&lt;(...)</code> / <code>&gt;(...)</code> Bash process substitution universally refused.</li>",
  "<li><strong>cmd.exe metacharacters</strong> — <code>&amp;</code>, <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>^</code>, <code>%</code>, <code>\"</code>, <code>'</code>, <code>(</code>, <code>)</code>, <code>,</code>, <code>;</code>, <code>=</code>.</li>",
  "<li><strong>Newline / NUL injection</strong> — line-splitting in scripts; universally refused.</li>",
  "<li><strong>$VAR parameter expansion</strong> — bare-form refused at strict; audited at balanced.</li>",
  "<li><strong>Leading-hyphen option-flag injection</strong> — operator may opt-in to refuse args starting with <code>-</code> to defend against <code>-rf</code> / <code>--exec</code> shapes that are interpreted as option flags by the target binary.</li>",
  "<li><strong>BIDI / null / control / zero-width</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>",
  "</ul>",
  "<h2 id=\"guard-shell-profiles\">Profiles <a class=\"anchor\" href=\"#guard-shell-profiles\">#</a></h2>",
  "<p><strong>strict</strong> — refuse everything in the catalog.</p>",
  "<p><strong>balanced</strong> — universal-refuse class still refused (substitutions / newline / BIDI / null); audit POSIX + cmd metacharacters + leading hyphen.</p>",
  "<p><strong>permissive</strong> — universal-refuse class still refused; metacharacters audited; allow leading hyphen.</p>",
  "<h2 id=\"guard-shell-compliance\">Compliance postures <a class=\"anchor\" href=\"#guard-shell-compliance\">#</a></h2>",
  "<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>",
  "<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>",
  "<h2 id=\"guard-regex\">Regex pattern safety <a class=\"anchor\" href=\"#guard-regex\">#</a></h2>",
  "<p><code>b.guardRegex</code> validates user-supplied regex pattern strings for catastrophic-backtracking (ReDoS) shapes BEFORE compilation. KIND=\"identifier\" — consumes <code>ctx.identifier</code> (or <code>ctx.pattern</code>). Threat catalog: nested quantifiers (canonical ReDoS class — CVE-2024-21538 cross-spawn / CVE-2022-25929), alternation with quantifier, bounded-repeat upper-bound overflow, lookaround with internal quantifier, oversized pattern, BIDI / null / control / zero-width universal refuse.</p>",
  "<h2 id=\"guard-regex-validate\">b.guardRegex.validate(pattern, opts) <a class=\"anchor\" href=\"#guard-regex-validate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:                 \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:       \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  nestedQuantPolicy:       \"reject\" | \"allow\",        // refused at every profile",
  "  alternationQuantPolicy:  \"reject\" | \"audit\" | \"allow\",",
  "  boundedRepeatPolicy:     \"reject\" | \"audit\" | \"allow\",",
  "  lookaroundQuantPolicy:   \"reject\" | \"audit\" | \"allow\",",
  "  bidiPolicy:              \"reject\" | \"allow\",        // refused at every profile",
  "  controlPolicy:           \"reject\" | \"allow\",        // refused at every profile",
  "  nullBytePolicy:          \"reject\" | \"allow\",        // refused at every profile",
  "  zeroWidthPolicy:         \"reject\" | \"allow\",        // refused at every profile",
  "  maxBoundedRepeat:        number,                     // strict 100 / balanced 1000 / permissive 10000",
  "  maxPatternBytes:         number,                     // 1 / 2 / 8 KiB",
  "  maxBytes:                number,                     // total input cap",
  "}</code></pre>",
  "<p>Validates a regex pattern string. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>empty</code>, <code>pattern-cap</code>, <code>nested-quantifier</code>, <code>alternation-quantifier</code>, <code>lookaround-quantifier</code>, <code>bounded-repeat-cap</code>, plus the universal codepoint-class kinds.</p>",
  "<pre><code class=\"language-javascript\">b.guardRegex.validate(\"^[a-z]+$\", { profile: \"strict\" });",
  "// → { ok: true, issues: [] }",
  "",
  "// Canonical ReDoS — universal refuse.",
  "b.guardRegex.validate(\"(a+)+b\",  { profile: \"strict\" }); // nested-quantifier",
  "",
  "// Bounded repeat overflow.",
  "b.guardRegex.validate(\"a{500}\",  { profile: \"strict\" }); // bounded-repeat-cap",
  "</code></pre>",
  "<h2 id=\"guard-regex-sanitize\">b.guardRegex.sanitize(pattern, opts) <a class=\"anchor\" href=\"#guard-regex-sanitize\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:           \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture: \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "}</code></pre>",
  "<p>Regex patterns can't be repaired — <code>sanitize</code> either passes through valid input or throws.</p>",
  "<pre><code class=\"language-javascript\">var pattern = \"^[a-z][a-z0-9]*$\";",
  "var safe = b.guardRegex.sanitize(pattern, { profile: \"strict\" });",
  "// safe === pattern when validation passes; throws otherwise.</code></pre>",
  "<h2 id=\"guard-regex-gate\">b.guardRegex.gate(opts) <a class=\"anchor\" href=\"#guard-regex-gate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:               \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:     \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  mode:                  \"enforce\" | \"warn-only\" | \"shadow\" | \"audit-only\" | \"log-only\" | \"canary\",",
  "  audit:                 b.audit,",
  "  observability:         b.observability,",
  "  forensicEvidenceStore: { put }, forensicSnippetBytes: number,",
  "  cache:                 b.cache, cacheTtlMs: number,",
  "  maxRuntimeMs:          number,",
  "}</code></pre>",
  "<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.pattern</code>); empty / missing pattern resolves to <code>{ ok: true, action: \"serve\" }</code>.</p>",
  "<pre><code class=\"language-javascript\">var regexGate = b.guardRegex.gate({ profile: \"strict\", audit: b.audit });",
  "",
  "app.post(\"/admin/route-rules\", async function (req, res) {",
  "  var rv = await regexGate.check({ identifier: req.body.pathRegex });",
  "  if (!rv.ok) return res.status(400).json({ refused: rv.issues });",
  "  // ... safe to compile new RegExp(req.body.pathRegex) downstream ...",
  "});</code></pre>",
  "<h2 id=\"guard-regex-threat-catalog\">Threat catalog <a class=\"anchor\" href=\"#guard-regex-threat-catalog\">#</a></h2>",
  "<ul>",
  "<li><strong>Nested quantifiers</strong> — <code>(a+)+</code>, <code>(a*)*</code>, <code>(.+)+</code>. The classic ReDoS shape; universally refused. CVE-2024-21538 (cross-spawn) and CVE-2022-25929 (chartjs-adapter-luxon) are recent prominent examples.</li>",
  "<li><strong>Alternation with quantifier</strong> — <code>(a|b)+</code> style strings; alternation overlap may amplify search paths.</li>",
  "<li><strong>Bounded repeat overflow</strong> — <code>a{500}</code>, <code>a{1,9999}</code>; operator-tunable upper-bound via <code>maxBoundedRepeat</code> (strict 100 / balanced 1000 / permissive 10000).</li>",
  "<li><strong>Lookaround with internal quantifier</strong> — <code>(?=.*+)</code>, <code>(?!a*)</code>; catastrophic in some engines.</li>",
  "<li><strong>Excessive pattern length</strong> — defense against parser DoS.</li>",
  "<li><strong>BIDI / null / control / zero-width</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>",
  "</ul>",
  "<h2 id=\"guard-regex-profiles\">Profiles <a class=\"anchor\" href=\"#guard-regex-profiles\">#</a></h2>",
  "<p><strong>strict</strong> — refuse everything (nested quants always; alternation-quant, bounded-repeat overflow, lookaround-quant refused; max bounded repeat 100).</p>",
  "<p><strong>balanced</strong> — nested quants still refused; audit alternation-quant + bounded-repeat + lookaround-quant; max bounded repeat 1000.</p>",
  "<p><strong>permissive</strong> — universal-refuse class still refused (nested quants, codepoint class); allow alternation-quant; audit bounded-repeat + lookaround-quant; max bounded repeat 10000.</p>",
  "<h2 id=\"guard-regex-compliance\">Compliance postures <a class=\"anchor\" href=\"#guard-regex-compliance\">#</a></h2>",
  "<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>",
  "<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>",
  "<h2 id=\"guard-jsonpath\">JSONPath safety <a class=\"anchor\" href=\"#guard-jsonpath\">#</a></h2>",
  "<p><code>b.guardJsonpath</code> validates user-supplied JSONPath strings (RFC 9535) before they're handed to a JSONPath evaluator. Many JSONPath libraries route filter / script expressions through dynamic-code execution, turning a query path into an RCE primitive. KIND=\"identifier\" — consumes <code>ctx.identifier</code> (or <code>ctx.jsonpath</code>). Threat catalog: filter expression <code>?(...)</code> (dynamic-code-execution class), script expression <code>(@.x)</code>, JS-source hints, recursive-descent depth bombs, excessive bracket nesting, oversized pattern, BIDI / null / control / zero-width universal refuse.</p>",
  "<h2 id=\"guard-jsonpath-validate\">b.guardJsonpath.validate(pattern, opts) <a class=\"anchor\" href=\"#guard-jsonpath-validate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:                  \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:        \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  filterExprPolicy:         \"reject\" | \"allow\",      // refused at every profile (RCE class)",
  "  scriptExprPolicy:         \"reject\" | \"audit\" | \"allow\",",
  "  dynamicHintPolicy:        \"reject\" | \"allow\",      // refused at every profile (RCE class)",
  "  bracketNestingPolicy:     \"reject\" | \"audit\" | \"allow\",",
  "  recursiveDescentPolicy:   \"reject\" | \"audit\" | \"allow\",",
  "  bidiPolicy:               \"reject\" | \"allow\",      // refused at every profile",
  "  controlPolicy:            \"reject\" | \"allow\",      // refused at every profile",
  "  nullBytePolicy:           \"reject\" | \"allow\",      // refused at every profile",
  "  zeroWidthPolicy:          \"reject\" | \"allow\",      // refused at every profile",
  "  maxRecursiveDescents:     number,                   // default 2 / 4 / 16",
  "  maxPatternBytes:          number,                   // default 1 / 2 / 8 KiB",
  "  maxBytes:                 number,                   // total cap",
  "}</code></pre>",
  "<p>Validates a JSONPath string. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>empty</code>, <code>pattern-cap</code>, <code>filter-expression</code>, <code>script-expression</code>, <code>dynamic-hint</code>, <code>bracket-nesting</code>, <code>recursive-descent-cap</code>, plus the universal codepoint-class kinds.</p>",
  "<pre><code class=\"language-javascript\">b.guardJsonpath.validate(\"$.users[*].name\", { profile: \"strict\" });",
  "// → { ok: true, issues: [] }",
  "",
  "// Filter expression — dynamic-code-execution class.",
  "b.guardJsonpath.validate(\"$.x[?(@.age > 18)]\", { profile: \"strict\" });",
  "// → ok=false; filter-expression",
  "</code></pre>",
  "<h2 id=\"guard-jsonpath-sanitize\">b.guardJsonpath.sanitize(pattern, opts) <a class=\"anchor\" href=\"#guard-jsonpath-sanitize\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:           \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture: \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "}</code></pre>",
  "<p>JSONPath strings can't be repaired — <code>sanitize</code> either passes through valid input or throws.</p>",
  "<pre><code class=\"language-javascript\">var pattern = \"$.users[*].name\";",
  "var safe = b.guardJsonpath.sanitize(pattern, { profile: \"strict\" });",
  "// safe === pattern when validation passes; throws otherwise.</code></pre>",
  "<h2 id=\"guard-jsonpath-gate\">b.guardJsonpath.gate(opts) <a class=\"anchor\" href=\"#guard-jsonpath-gate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:               \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:     \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  mode:                  \"enforce\" | \"warn-only\" | \"shadow\" | \"audit-only\" | \"log-only\" | \"canary\",",
  "  audit:                 b.audit,",
  "  observability:         b.observability,",
  "  forensicEvidenceStore: { put }, forensicSnippetBytes: number,",
  "  cache:                 b.cache, cacheTtlMs: number,",
  "  maxRuntimeMs:          number,",
  "}</code></pre>",
  "<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.jsonpath</code>); empty / missing pattern resolves to <code>{ ok: true, action: \"serve\" }</code>.</p>",
  "<pre><code class=\"language-javascript\">var jpGate = b.guardJsonpath.gate({ profile: \"strict\", audit: b.audit });",
  "",
  "app.post(\"/api/query\", async function (req, res) {",
  "  var rv = await jpGate.check({ identifier: req.body.path });",
  "  if (!rv.ok) return res.status(400).json({ refused: rv.issues });",
  "  // ... safe to evaluate JSONPath via operator's chosen library ...",
  "});</code></pre>",
  "<h2 id=\"guard-jsonpath-threat-catalog\">Threat catalog <a class=\"anchor\" href=\"#guard-jsonpath-threat-catalog\">#</a></h2>",
  "<ul>",
  "<li><strong>Filter expression <code>?(...)</code></strong> — dynamic-code-execution class in legacy implementations. Universally refused at every profile.</li>",
  "<li><strong>JS-source hints</strong> — operator-supplied path containing tokens that only appear in code-injection attempts (dynamic-code-exec keyword, constructor invocation keyword, function-declaration keyword, arrow-function arrow, statement-separator semicolon).</li>",
  "<li><strong>Script expression <code>(@.x)</code></strong> style — RFC 9535 doesn't define it but many implementations support it as alias for filter; same RCE class.</li>",
  "<li><strong>Recursive-descent depth bombs</strong> — <code>..[*]</code> repeated > N times amplifies traversal cost.</li>",
  "<li><strong>Excessive bracket nesting</strong> — parser DoS shape.</li>",
  "<li><strong>Excessive pattern length</strong> — parser DoS defense.</li>",
  "<li><strong>BIDI / null / control / zero-width</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>",
  "</ul>",
  "<h2 id=\"guard-jsonpath-profiles\">Profiles <a class=\"anchor\" href=\"#guard-jsonpath-profiles\">#</a></h2>",
  "<p><strong>strict</strong> — refuse everything (RCE class always; bracket nesting + recursive descent refused; max recursive descents 2).</p>",
  "<p><strong>balanced</strong> — RCE class still refused; audit bracket nesting + recursive descent (max 4).</p>",
  "<p><strong>permissive</strong> — RCE class still refused; allow recursive descent up to 16; audit bracket nesting.</p>",
  "<h2 id=\"guard-jsonpath-compliance\">Compliance postures <a class=\"anchor\" href=\"#guard-jsonpath-compliance\">#</a></h2>",
  "<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>",
  "<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>",
  "<h2 id=\"guard-template\">Template-injection safety <a class=\"anchor\" href=\"#guard-template\">#</a></h2>",
  "<p><code>b.guardTemplate</code> detects Server-Side Template Injection (SSTI) shapes in user-input strings before they're rendered through any template engine. Refused by default at every profile — operator-untrusted input rarely legitimately contains template-engine syntax. KIND=\"identifier\" — consumes <code>ctx.identifier</code> (or <code>ctx.text</code>). Threat catalog covers Jinja / Django / Twig / Liquid / Handlebars / AngularJS, ERB / Tornado, Pug, Mako / Velocity / Tornado, Velocity directives, plus BIDI / null / control / zero-width universal refuse.</p>",
  "<h2 id=\"guard-template-validate\">b.guardTemplate.validate(text, opts) <a class=\"anchor\" href=\"#guard-template-validate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:                    \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:          \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  jinjaPolicy:                \"reject\" | \"allow\",        // refused at every profile",
  "  erbPolicy:                  \"reject\" | \"allow\",        // refused at every profile",
  "  pugPolicy:                  \"reject\" | \"allow\",        // refused at every profile",
  "  dollarBracePolicy:          \"reject\" | \"audit\" | \"allow\",",
  "  velocityDirectivePolicy:    \"reject\" | \"audit\" | \"allow\",",
  "  bidiPolicy:                 \"reject\" | \"allow\",        // refused at every profile",
  "  controlPolicy:              \"reject\" | \"allow\",        // refused at every profile",
  "  nullBytePolicy:             \"reject\" | \"allow\",        // refused at every profile",
  "  zeroWidthPolicy:            \"reject\" | \"allow\",        // refused at every profile",
  "  maxBytes:                   number,                     // default 64 / 128 / 512 KiB",
  "}</code></pre>",
  "<p>Validates an input string against the SSTI catalog. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>input-cap</code>, <code>jinja-expression</code>, <code>jinja-statement</code>, <code>erb-expression</code>, <code>pug-interpolation</code>, <code>dollar-brace</code>, <code>velocity-directive</code>, plus the universal codepoint-class kinds.</p>",
  "<pre><code class=\"language-javascript\">b.guardTemplate.validate(\"Hello world\", { profile: \"strict\" });",
  "// → { ok: true, issues: [] }",
  "",
  "// Jinja / Django / Twig / Handlebars SSTI probe.",
  "b.guardTemplate.validate(\"Hello {{7*7}}\", { profile: \"strict\" });",
  "// → ok=false; jinja-expression",
  "",
  "// ERB / Tornado.",
  "b.guardTemplate.validate(\"Hello <%= 7*7 %>\", { profile: \"strict\" });",
  "// → ok=false; erb-expression",
  "",
  "// Pug.",
  "b.guardTemplate.validate(\"Hello #{7*7}\", { profile: \"strict\" });",
  "// → ok=false; pug-interpolation",
  "</code></pre>",
  "<h2 id=\"guard-template-sanitize\">b.guardTemplate.sanitize(text, opts) <a class=\"anchor\" href=\"#guard-template-sanitize\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:           \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture: \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "}</code></pre>",
  "<p>Template-injection shapes can't be repaired without changing semantics — <code>sanitize</code> either passes through valid input or throws.</p>",
  "<pre><code class=\"language-javascript\">var text = \"Hello world\";",
  "var safe = b.guardTemplate.sanitize(text, { profile: \"strict\" });",
  "// safe === text when validation passes; throws otherwise.</code></pre>",
  "<h2 id=\"guard-template-gate\">b.guardTemplate.gate(opts) <a class=\"anchor\" href=\"#guard-template-gate\">#</a></h2>",
  "<pre><code class=\"language-javascript\">{",
  "  profile:               \"strict\" | \"balanced\" | \"permissive\",",
  "  compliancePosture:     \"hipaa\" | \"pci-dss\" | \"gdpr\" | \"soc2\",",
  "  mode:                  \"enforce\" | \"warn-only\" | \"shadow\" | \"audit-only\" | \"log-only\" | \"canary\",",
  "  audit:                 b.audit,",
  "  observability:         b.observability,",
  "  forensicEvidenceStore: { put }, forensicSnippetBytes: number,",
  "  cache:                 b.cache, cacheTtlMs: number,",
  "  maxRuntimeMs:          number,",
  "}</code></pre>",
  "<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.text</code>); empty / missing input resolves to <code>{ ok: true, action: \"serve\" }</code>.</p>",
  "<pre><code class=\"language-javascript\">var tplGate = b.guardTemplate.gate({ profile: \"strict\", audit: b.audit });",
  "",
  "app.post(\"/api/comments\", async function (req, res) {",
  "  var rv = await tplGate.check({ identifier: req.body.text });",
  "  if (!rv.ok) return res.status(400).json({ refused: rv.issues });",
  "  // ... safe to persist + render through any template engine ...",
  "});</code></pre>",
  "<h2 id=\"guard-template-threat-catalog\">Threat catalog <a class=\"anchor\" href=\"#guard-template-threat-catalog\">#</a></h2>",
  "<ul>",
  "<li><strong>Jinja2 / Django / Twig / Liquid / Handlebars / AngularJS</strong> — <code>{{...}}</code> + <code>{%...%}</code>. Recent CVEs: CVE-2024-22195 (Jinja xml_attr filter), CVE-2024-26139 (Bottle), CVE-2024-23348 (Pyrogram).</li>",
  "<li><strong>ERB / Tornado</strong> — <code>&lt;%...%&gt;</code> and <code>&lt;%=...%&gt;</code>.</li>",
  "<li><strong>Pug</strong> — <code>#{...}</code> interpolation, <code>!{...}</code> raw-HTML interpolation.</li>",
  "<li><strong>Mako / Velocity / Tornado</strong> — <code>${...}</code> interpolation.</li>",
  "<li><strong>Velocity directive</strong> — <code>#set</code>, <code>#if</code>, <code>#foreach</code>, <code>#parse</code>, <code>#include</code>.</li>",
  "<li><strong>BIDI / null / control / zero-width</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>",
  "</ul>",
  "<h2 id=\"guard-template-profiles\">Profiles <a class=\"anchor\" href=\"#guard-template-profiles\">#</a></h2>",
  "<p><strong>strict</strong> — refuse everything in the catalog.</p>",
  "<p><strong>balanced</strong> — Jinja / ERB / Pug / Velocity directives still refused; audit <code>${...}</code> (could be JS template literal in some legitimate operator contexts).</p>",
  "<p><strong>permissive</strong> — Jinja / ERB / Pug still refused (universal SSTI); audit <code>${...}</code> + Velocity directives.</p>",
  "<h2 id=\"guard-template-compliance\">Compliance postures <a class=\"anchor\" href=\"#guard-template-compliance\">#</a></h2>",
  "<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (512 / 512 / 1024 bytes).</p>",
  "<p><strong>gdpr</strong> — balanced overlay + 256-byte forensic snippets.</p>",
  "<h2 id=\"guard-helper-surface\">Shared helper surface <a class=\"anchor\" href=\"#guard-helper-surface\">#</a></h2>",
  "<p>Every guard in this category also exposes the shared helper surface used by operators and by <code>b.guardAll</code>: profile composition, compliance posture lookup, and local rule-pack registration.</p>",
  "<h2 id=\"shell-build-profile\">b.guardShell.buildProfile(profileOptions) <a class=\"anchor\" href=\"#shell-build-profile\">#</a></h2>",
  "<p><code>b.guardShell.buildProfile</code> composes one of the guard's named profiles with additional operator extensions, overrides, and removals. Use it when a deployment wants to inspect the exact policy object that will feed validation or gate creation before wiring it into a host primitive.</p>",
  "<pre><code class=\"language-javascript\">var profile = b.guardShell.buildProfile({",
  "  baseProfile: \"strict\",",
  "  overrides: { mode: \"enforce\" },",
  "});",
  "typeof profile; // \"object\"</code></pre>",
  "<h2 id=\"shell-compliance-posture\">b.guardShell.compliancePosture(name) <a class=\"anchor\" href=\"#shell-compliance-posture\">#</a></h2>",
  "<p><code>b.guardShell.compliancePosture</code> returns the guard-specific policy overlay for a named posture such as <code>soc2</code>, <code>gdpr</code>, <code>hipaa</code>, or <code>pci-dss</code>. Unknown posture names fail immediately so compliance wiring cannot silently fall back to a weaker profile.</p>",
  "<pre><code class=\"language-javascript\">var posture = b.guardShell.compliancePosture(\"soc2\");",
  "typeof posture; // \"object\"</code></pre>",
  "<h2 id=\"shell-load-rule-pack\">b.guardShell.loadRulePack(pack) <a class=\"anchor\" href=\"#shell-load-rule-pack\">#</a></h2>",
  "<p><code>b.guardShell.loadRulePack</code> registers an operator rule pack for this guard and returns the accepted pack. Rule packs give deployments a stable place to add local detection rules without forking the framework primitive.</p>",
  "<pre><code class=\"language-javascript\">var pack = b.guardShell.loadRulePack({",
  "  id: \"docs-shell-rule-pack\",",
  "  rules: [],",
  "});",
  "pack.id; // \"docs-shell-rule-pack\"</code></pre>",
  "<h2 id=\"regex-build-profile\">b.guardRegex.buildProfile(profileOptions) <a class=\"anchor\" href=\"#regex-build-profile\">#</a></h2>",
  "<p><code>b.guardRegex.buildProfile</code> composes one of the guard's named profiles with additional operator extensions, overrides, and removals. Use it when a deployment wants to inspect the exact policy object that will feed validation or gate creation before wiring it into a host primitive.</p>",
  "<pre><code class=\"language-javascript\">var profile = b.guardRegex.buildProfile({",
  "  baseProfile: \"strict\",",
  "  overrides: { mode: \"enforce\" },",
  "});",
  "typeof profile; // \"object\"</code></pre>",
  "<h2 id=\"regex-compliance-posture\">b.guardRegex.compliancePosture(name) <a class=\"anchor\" href=\"#regex-compliance-posture\">#</a></h2>",
  "<p><code>b.guardRegex.compliancePosture</code> returns the guard-specific policy overlay for a named posture such as <code>soc2</code>, <code>gdpr</code>, <code>hipaa</code>, or <code>pci-dss</code>. Unknown posture names fail immediately so compliance wiring cannot silently fall back to a weaker profile.</p>",
  "<pre><code class=\"language-javascript\">var posture = b.guardRegex.compliancePosture(\"soc2\");",
  "typeof posture; // \"object\"</code></pre>",
  "<h2 id=\"regex-load-rule-pack\">b.guardRegex.loadRulePack(pack) <a class=\"anchor\" href=\"#regex-load-rule-pack\">#</a></h2>",
  "<p><code>b.guardRegex.loadRulePack</code> registers an operator rule pack for this guard and returns the accepted pack. Rule packs give deployments a stable place to add local detection rules without forking the framework primitive.</p>",
  "<pre><code class=\"language-javascript\">var pack = b.guardRegex.loadRulePack({",
  "  id: \"docs-regex-rule-pack\",",
  "  rules: [],",
  "});",
  "pack.id; // \"docs-regex-rule-pack\"</code></pre>",
  "<h2 id=\"jsonpath-build-profile\">b.guardJsonpath.buildProfile(profileOptions) <a class=\"anchor\" href=\"#jsonpath-build-profile\">#</a></h2>",
  "<p><code>b.guardJsonpath.buildProfile</code> composes one of the guard's named profiles with additional operator extensions, overrides, and removals. Use it when a deployment wants to inspect the exact policy object that will feed validation or gate creation before wiring it into a host primitive.</p>",
  "<pre><code class=\"language-javascript\">var profile = b.guardJsonpath.buildProfile({",
  "  baseProfile: \"strict\",",
  "  overrides: { mode: \"enforce\" },",
  "});",
  "typeof profile; // \"object\"</code></pre>",
  "<h2 id=\"jsonpath-compliance-posture\">b.guardJsonpath.compliancePosture(name) <a class=\"anchor\" href=\"#jsonpath-compliance-posture\">#</a></h2>",
  "<p><code>b.guardJsonpath.compliancePosture</code> returns the guard-specific policy overlay for a named posture such as <code>soc2</code>, <code>gdpr</code>, <code>hipaa</code>, or <code>pci-dss</code>. Unknown posture names fail immediately so compliance wiring cannot silently fall back to a weaker profile.</p>",
  "<pre><code class=\"language-javascript\">var posture = b.guardJsonpath.compliancePosture(\"soc2\");",
  "typeof posture; // \"object\"</code></pre>",
  "<h2 id=\"jsonpath-load-rule-pack\">b.guardJsonpath.loadRulePack(pack) <a class=\"anchor\" href=\"#jsonpath-load-rule-pack\">#</a></h2>",
  "<p><code>b.guardJsonpath.loadRulePack</code> registers an operator rule pack for this guard and returns the accepted pack. Rule packs give deployments a stable place to add local detection rules without forking the framework primitive.</p>",
  "<pre><code class=\"language-javascript\">var pack = b.guardJsonpath.loadRulePack({",
  "  id: \"docs-jsonpath-rule-pack\",",
  "  rules: [],",
  "});",
  "pack.id; // \"docs-jsonpath-rule-pack\"</code></pre>",
  "<h2 id=\"template-build-profile\">b.guardTemplate.buildProfile(profileOptions) <a class=\"anchor\" href=\"#template-build-profile\">#</a></h2>",
  "<p><code>b.guardTemplate.buildProfile</code> composes one of the guard's named profiles with additional operator extensions, overrides, and removals. Use it when a deployment wants to inspect the exact policy object that will feed validation or gate creation before wiring it into a host primitive.</p>",
  "<pre><code class=\"language-javascript\">var profile = b.guardTemplate.buildProfile({",
  "  baseProfile: \"strict\",",
  "  overrides: { mode: \"enforce\" },",
  "});",
  "typeof profile; // \"object\"</code></pre>",
  "<h2 id=\"template-compliance-posture\">b.guardTemplate.compliancePosture(name) <a class=\"anchor\" href=\"#template-compliance-posture\">#</a></h2>",
  "<p><code>b.guardTemplate.compliancePosture</code> returns the guard-specific policy overlay for a named posture such as <code>soc2</code>, <code>gdpr</code>, <code>hipaa</code>, or <code>pci-dss</code>. Unknown posture names fail immediately so compliance wiring cannot silently fall back to a weaker profile.</p>",
  "<pre><code class=\"language-javascript\">var posture = b.guardTemplate.compliancePosture(\"soc2\");",
  "typeof posture; // \"object\"</code></pre>",
  "<h2 id=\"template-load-rule-pack\">b.guardTemplate.loadRulePack(pack) <a class=\"anchor\" href=\"#template-load-rule-pack\">#</a></h2>",
  "<p><code>b.guardTemplate.loadRulePack</code> registers an operator rule pack for this guard and returns the accepted pack. Rule packs give deployments a stable place to add local detection rules without forking the framework primitive.</p>",
  "<pre><code class=\"language-javascript\">var pack = b.guardTemplate.loadRulePack({",
  "  id: \"docs-template-rule-pack\",",
  "  rules: [],",
  "});",
  "pack.id; // \"docs-template-rule-pack\"</code></pre>",
  ].join("\n"),
};
