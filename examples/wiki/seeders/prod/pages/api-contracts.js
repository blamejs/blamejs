"use strict";

module.exports = {
  slug:  "api-contracts",
  title: "API Contracts",
  body: [
  '<h1>API Contracts</h1>',
  '<p>Schema and transport primitives for publishing machine-readable API contracts, guarding protocol ingress, and speaking realtime protocols without npm runtime dependencies.</p>',

  '<h2 id="gate-contract-define">b.gateContract.defineGate(options) <a class="anchor" href="#gate-contract-define">#</a></h2>',
  '<p>Builds the uniform gate object used by guard primitives and host surfaces. The returned gate wraps an operator check function with mode posture, hooks, audit emission, observability, optional cache lookup, runtime caps, and forensic metadata.</p>',
  '<pre><code class="language-javascript">var gate = b.gateContract.defineGate({',
  '  name: "wiki:uploads",',
  '  check: async function (ctx) {',
  '    return ctx.bytes ? { ok: true, action: "serve" } : { ok: false, action: "refuse" };',
  '  },',
  '});',
  'typeof gate.check; // "function"</code></pre>',

  '<h2 id="gate-contract-validate">b.gateContract.validateGateShape(gate, label) <a class="anchor" href="#gate-contract-validate">#</a></h2>',
  '<p>Fails fast when an operator-supplied gate is missing the contract shape expected by upload, static-serving, mail, object-store, and other byte-boundary hosts.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.validateGateShape; // "function"</code></pre>',

  '<h2 id="gate-contract-run">b.gateContract.runGate(gate, ctx) <a class="anchor" href="#gate-contract-run">#</a></h2>',
  '<p>Executes a single gate and normalizes missing gates to a serve decision so host primitives can accept optional content-safety policies without branching.</p>',
  '<pre><code class="language-javascript">var decision = await b.gateContract.runGate(null, { bytes: "ok" });',
  'decision.action; // "serve"</code></pre>',

  '<h2 id="gate-contract-compose">b.gateContract.composeGates(gates, options) <a class="anchor" href="#gate-contract-compose">#</a></h2>',
  '<p>Chains gates left-to-right. The first refusal wins, and sanitize decisions can feed their sanitized bytes to the next gate.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.composeGates; // "function"</code></pre>',

  '<h2 id="gate-contract-mux">b.gateContract.multiplexGates(gateMap, options) <a class="anchor" href="#gate-contract-mux">#</a></h2>',
  '<p>Dispatches by file extension, with <code>default</code> as the fallback entry. This is the host-side primitive behind extension-aware upload and static-serving safety policies.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.multiplexGates; // "function"</code></pre>',

  '<h2 id="gate-contract-content-type">b.gateContract.contentTypeMux(gateMap, options) <a class="anchor" href="#gate-contract-content-type">#</a></h2>',
  '<p>Dispatches by bare Content-Type after stripping parameters such as <code>; charset=utf-8</code>.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.contentTypeMux; // "function"</code></pre>',

  '<h2 id="gate-contract-actor-tier">b.gateContract.byActorTier(gateMap, options) <a class="anchor" href="#gate-contract-actor-tier">#</a></h2>',
  '<p>Chooses a gate from <code>ctx.actor.tier</code>, letting operators run stricter checks for anonymous or free-tier actors while retaining a default path.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.byActorTier; // "function"</code></pre>',

  '<h2 id="gate-contract-route">b.gateContract.byRoute(gateMap, options) <a class="anchor" href="#gate-contract-route">#</a></h2>',
  '<p>Chooses a gate by exact route or simple prefix glob such as <code>/admin/*</code>.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.byRoute; // "function"</code></pre>',

  '<h2 id="gate-contract-direction">b.gateContract.byDirection(gateMap, options) <a class="anchor" href="#gate-contract-direction">#</a></h2>',
  '<p>Selects separate gates for inbound and outbound byte flows, useful when upload scanning and response filtering have different policies.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.byDirection; // "function"</code></pre>',

  '<h2 id="gate-contract-shadow">b.gateContract.shadowMode(primary, candidate, options) <a class="anchor" href="#gate-contract-shadow">#</a></h2>',
  '<p>Honors the primary gate while running a candidate gate in the background and emitting divergence telemetry. This supports staged policy rollouts without changing request outcomes.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.shadowMode; // "function"</code></pre>',

  '<h2 id="gate-contract-canary">b.gateContract.canaryGate(gate, options) <a class="anchor" href="#gate-contract-canary">#</a></h2>',
  '<p>Enforces a wrapped gate for a configured sample and downgrades the rest to warnings, giving operators a measured rollout path for stricter content policies.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.canaryGate; // "function"</code></pre>',

  '<h2 id="gate-contract-cache">b.gateContract.cachingGate(gate, options) <a class="anchor" href="#gate-contract-cache">#</a></h2>',
  '<p>Wraps a gate with an explicit shared cache backend. Use it when multiple host primitives should reuse decisions by forensic hash instead of keeping cache state per gate instance.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.cachingGate; // "function"</code></pre>',

  '<h2 id="gate-contract-worker">b.gateContract.workerThreadGate(gate, options) <a class="anchor" href="#gate-contract-worker">#</a></h2>',
  '<p>Adapts a gate to an operator-supplied worker interface so CPU-heavy checks can be isolated from the request thread.</p>',
  '<pre><code class="language-javascript">typeof b.gateContract.workerThreadGate; // "function"</code></pre>',

  '<h2 id="gate-contract-profile">b.gateContract.buildProfile(options) <a class="anchor" href="#gate-contract-profile">#</a></h2>',
  '<p>Composes named profiles with inheritance, overrides, removals, and cycle detection. Guard primitives use it to make local profiles and operator overlays behave consistently.</p>',
  '<pre><code class="language-javascript">var profile = b.gateContract.buildProfile({',
  '  baseProfile: "strict",',
  '  resolveProfile: function (name) { return name === "strict" ? { maxBytes: 1024 } : null; },',
  '});',
  'profile.maxBytes; // 1024</code></pre>',

  '<h2 id="gate-contract-posture">b.gateContract.resolveProfileAndPosture(options, config) <a class="anchor" href="#gate-contract-posture">#</a></h2>',
  '<p>Overlays a guard profile and compliance posture onto guard defaults. This is the common resolver behind guard-specific <code>create</code> and <code>gate</code> factories.</p>',
  '<pre><code class="language-javascript">var resolved = b.gateContract.resolveProfileAndPosture({ profile: "strict" }, {',
  '  profiles: { strict: { maxBytes: 1024 } },',
  '  defaults: { mode: "enforce" },',
  '});',
  'resolved.maxBytes; // 1024</code></pre>',

  '<h2 id="gate-contract-issues">b.gateContract.runIssueValidator(input, options, detector) <a class="anchor" href="#gate-contract-issues">#</a></h2>',
  '<p>Normalizes string or Buffer input for guard <code>validate</code> entry points and returns the canonical <code>{ ok, issues }</code> result.</p>',
  '<pre><code class="language-javascript">var result = b.gateContract.runIssueValidator("hello", {}, function () { return []; });',
  'result.ok; // true</code></pre>',

  '<h2 id="gate-contract-build-guard">b.gateContract.buildGuardGate(name, options, check) <a class="anchor" href="#gate-contract-build-guard">#</a></h2>',
  '<p>Creates a guard gate while forwarding shared options such as hooks, audit, observability, cache, forensic store, and runtime cap into <code>defineGate</code>.</p>',
  '<pre><code class="language-javascript">var guardGate = b.gateContract.buildGuardGate("wiki:guard", {}, async function () {',
  '  return { ok: true, action: "serve" };',
  '});',
  'typeof guardGate.check; // "function"</code></pre>',

  '<h2 id="gate-contract-text">b.gateContract.extractBytesAsText(ctx) <a class="anchor" href="#gate-contract-text">#</a></h2>',
  '<p>Converts <code>ctx.bytes</code> from string or Buffer into text and returns an empty string when no bytes are present.</p>',
  '<pre><code class="language-javascript">b.gateContract.extractBytesAsText({ bytes: Buffer.from("abc") });',
  '// "abc"</code></pre>',

  '<h2 id="gate-contract-compliance">b.gateContract.lookupCompliancePosture(name, postures, errorFactory, prefix) <a class="anchor" href="#gate-contract-compliance">#</a></h2>',
  '<p>Looks up and clones a named compliance posture for a guard primitive, raising the guard-specific error when the posture is unknown.</p>',
  '<pre><code class="language-javascript">var posture = b.gateContract.lookupCompliancePosture("hipaa", { hipaa: { mode: "enforce" } },',
  '  b.gateContract.GateContractError.factory, "guard");',
  'posture.mode; // "enforce"</code></pre>',

  '<h2 id="gate-contract-rule-pack">b.gateContract.makeRulePackLoader(errorClass, prefix) <a class="anchor" href="#gate-contract-rule-pack">#</a></h2>',
  '<p>Creates a small in-memory rule-pack loader for guard primitives. Accepted packs are keyed by <code>id</code> and can be listed or fetched later.</p>',
  '<pre><code class="language-javascript">var loader = b.gateContract.makeRulePackLoader(b.gateContract.GateContractError, "guard");',
  'loader.load({ id: "local-rules", rules: [] });',
  'loader.list().length; // 1</code></pre>',

  '<h2 id="gate-contract-profile-builder">b.gateContract.makeProfileBuilder(profiles) <a class="anchor" href="#gate-contract-profile-builder">#</a></h2>',
  '<p>Returns a profile builder bound to a guard primitive\'s local profile map.</p>',
  '<pre><code class="language-javascript">var build = b.gateContract.makeProfileBuilder({ strict: { maxBytes: 1024 } });',
  'build({ baseProfile: "strict" }).maxBytes; // 1024</code></pre>',

  '<h2 id="gate-contract-bad-input">b.gateContract.badInputResultIfNotStringOrBuffer(input) <a class="anchor" href="#gate-contract-bad-input">#</a></h2>',
  '<p>Returns the canonical bad-input issue result for guard validators that must accept only strings or Buffers.</p>',
  '<pre><code class="language-javascript">var bad = b.gateContract.badInputResultIfNotStringOrBuffer({});',
  'bad.ok; // false</code></pre>',

  '<h2 id="gate-contract-aggregate">b.gateContract.aggregateIssues(issues) <a class="anchor" href="#gate-contract-aggregate">#</a></h2>',
  '<p>Converts a list of guard issues into the canonical validation result, with <code>ok</code> false when any issue is high or critical severity.</p>',
  '<pre><code class="language-javascript">b.gateContract.aggregateIssues([{ kind: "x", severity: "high" }]).ok;',
  '// false</code></pre>',

  '<h2 id="gate-contract-hooks">b.gateContract.composeHooks(hooks) <a class="anchor" href="#gate-contract-hooks">#</a></h2>',
  '<p>Chains operator hooks. Suppression and skip responses short-circuit; transformer-style hook results flow through the sequence.</p>',
  '<pre><code class="language-javascript">var hook = b.gateContract.composeHooks([async function () { return { tagged: true }; }]);',
  'typeof hook; // "function"</code></pre>',

  '<h2 id="gate-contract-summary">b.gateContract.summarizeIssues(issues) <a class="anchor" href="#gate-contract-summary">#</a></h2>',
  '<p>Projects full issue objects down to the audit-safe shape: kind, severity, and rule id. Snippets stay in the forensic evidence path instead of the audit row.</p>',
  '<pre><code class="language-javascript">var summary = b.gateContract.summarizeIssues([{ kind: "x", severity: "warn", ruleId: "r1", snippet: "secret" }]);',
  'summary[0].ruleId; // "r1"</code></pre>',

  '<h2 id="openapi">OpenAPI</h2>',
  '<h3>b.openapi.create(opts) <a class="anchor" href="#openapi-create">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  info:         object,      // required: { title, version }',
  '  servers:      array,       // optional OpenAPI server entries',
  '  externalDocs: object,      // optional externalDocs object',
  '  tags:         array,       // optional tag list',
  '  security:     array,       // optional document-level security requirements',
  '}</code></pre>',
  '<p>Builds an OpenAPI 3.1 document from an operator-authored contract. The builder intentionally does not infer routes from <code>b.router</code>; the published contract is often narrower than the internal route table.</p>',
  '<pre><code class="language-javascript">var api = b.openapi.create({',
  '  info: { title: "Wiki API", version: "1.0.0" },',
  '});',
  'api.path("GET", "/pages/{slug}", {',
  '  operationId: "getPage",',
  '  parameters: [{ name: "slug", in: "path", required: true, schema: { type: "string" } }],',
  '  responses: { "200": { description: "Page body" } },',
  '});',
  'var doc = api.toJson();',
  'doc.openapi; // "3.1.0"</code></pre>',

  '<h3>b.openapi.parse(input, opts?) <a class="anchor" href="#openapi-parse">#</a></h3>',
  '<p>Parses an OpenAPI document from JSON text or an object and validates the minimum document shape before downstream tooling consumes it.</p>',
  '<pre><code class="language-javascript">var parsed = b.openapi.parse({',
  '  openapi: "3.1.0",',
  '  info: { title: "Wiki API", version: "1.0.0" },',
  '  paths: {},',
  '});',
  'parsed.doc.info.title; // "Wiki API"</code></pre>',

  '<h3>b.openapi.schemaWalk(schema) <a class="anchor" href="#openapi-schema-walk">#</a></h3>',
  '<p>Converts <code>b.safeSchema</code>-style schemas into JSON Schema fragments for OpenAPI components.</p>',
  '<pre><code class="language-javascript">var jsonSchema = b.openapi.schemaWalk(',
  '  b.safeSchema.object({ id: b.safeSchema.string().uuid() })',
  ');',
  'jsonSchema.type; // "object"</code></pre>',

  '<h2 id="openapi-security-h2">OpenAPI security schemes</h2>',
  '<h3>b.openapi.security.bearer({ jwtBearer, description }) <a class="anchor" href="#openapi-security-bearer">#</a></h3>',
  '<p>Builds an HTTP Bearer security scheme for OpenAPI 3.1 <code>components.securitySchemes</code>. Setting <code>jwtBearer: true</code> records <code>bearerFormat: "JWT"</code> for documentation tools that emit JWT-aware client SDKs.</p>',
  '<pre><code class="language-javascript">var scheme = b.openapi.security.bearer({ jwtBearer: true });',
  'scheme.type;         // "http"',
  'scheme.scheme;       // "bearer"',
  'scheme.bearerFormat; // "JWT"</code></pre>',

  '<h3>b.openapi.security.basic({ description }) <a class="anchor" href="#openapi-security-basic">#</a></h3>',
  '<p>Builds an HTTP Basic security scheme. Operators usually pair this with TLS-only deployments and an opt-out flag for legacy partners; partners on modern channels should reach for <code>bearer</code>, <code>oauth2</code>, or <code>mtls</code>.</p>',
  '<pre><code class="language-javascript">var scheme = b.openapi.security.basic();',
  'scheme.scheme; // "basic"</code></pre>',

  '<h3>b.openapi.security.apiKey({ name, in, description }) <a class="anchor" href="#openapi-security-apikey">#</a></h3>',
  '<p>Builds an API-key security scheme. The <code>in</code> field must be one of <code>header</code>, <code>query</code>, or <code>cookie</code>; anything else throws <code>OpenApiError</code> at build time so a typo fails when the document is generated, not at consumer-validation time.</p>',
  '<pre><code class="language-javascript">var scheme = b.openapi.security.apiKey({ name: "X-API-Key", in: "header" });',
  'scheme.type; // "apiKey"',
  'scheme.in;   // "header"</code></pre>',

  '<h3>b.openapi.security.oauth2({ flows, description }) <a class="anchor" href="#openapi-security-oauth2">#</a></h3>',
  '<p>Builds an OAuth2 security scheme. <code>flows</code> may include any subset of <code>authorizationCode</code>, <code>clientCredentials</code>, <code>implicit</code>, and <code>password</code>; each flow validates its required URLs and a <code>scopes</code> object, and unknown flow names throw at build time.</p>',
  '<pre><code class="language-javascript">var scheme = b.openapi.security.oauth2({',
  '  flows: {',
  '    authorizationCode: {',
  '      authorizationUrl: "https://auth.example/oauth/authorize",',
  '      tokenUrl:         "https://auth.example/oauth/token",',
  '      scopes:           { "read:pages": "Read wiki pages" },',
  '    },',
  '  },',
  '});',
  'scheme.type; // "oauth2"</code></pre>',

  '<h3>b.openapi.security.openIdConnect({ url, description }) / b.openapi.security.mtls({ description }) / b.openapi.security.dpop({ description }) <a class="anchor" href="#openapi-security-misc">#</a></h3>',
  '<p>Builds the OpenID Connect discovery scheme (URL required), the OpenAPI 3.1 <code>mutualTLS</code> scheme for RFC 8705 deployments, and a DPoP-bound bearer scheme (RFC 9449) emitted as an HTTP scheme with the proof-of-possession requirement recorded in the description.</p>',
  '<pre><code class="language-javascript">var oidc = b.openapi.security.openIdConnect({ url: "https://auth.example/.well-known/openid-configuration" });',
  'oidc.type; // "openIdConnect"',
  'var mtls = b.openapi.security.mtls();',
  'mtls.type; // "mutualTLS"',
  'var dpop = b.openapi.security.dpop();',
  'dpop.scheme; // "dpop"</code></pre>',

  '<h3>b.openapi.toYaml(doc) <a class="anchor" href="#openapi-yaml">#</a></h3>',
  '<p>Serializes an OpenAPI 3.1 document into YAML for publishing to portals or partner-facing contract repositories. The serializer emits canonical YAML with quoted keys where necessary so the output round-trips through any conformant YAML parser.</p>',
  '<pre><code class="language-javascript">var api = b.openapi.create({',
  '  info: { title: "Wiki API", version: "1.0.0" },',
  '});',
  'var yaml = b.openapi.toYaml(api.toJson());',
  'yaml.indexOf("openapi:") >= 0; // true</code></pre>',

  '<h2 id="asyncapi">AsyncAPI</h2>',
  '<h3>b.asyncapi.create(opts) <a class="anchor" href="#asyncapi-create">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  info:               object,    // required: { title, version }',
  '  servers:            object,    // optional named server map',
  '  defaultContentType: string,    // default: "application/json"',
  '  security:           array,     // optional document security',
  '  externalDocs:       object,    // optional externalDocs object',
  '  tags:               array,     // optional tag list',
  '  id:                 string,    // optional document id',
  '}</code></pre>',
  '<p>Builds an AsyncAPI 3.0 document for pubsub, WebSocket, Kafka, MQTT, AMQP, or HTTP event surfaces. Operators publish the event contract they want partners to rely on, not every internal topic.</p>',
  '<pre><code class="language-javascript">var events = b.asyncapi.create({',
  '  info: { title: "Wiki Events", version: "1.0.0" },',
  '});',
  'events.channel("page.updated", { address: "page.updated" });',
  'events.operation("publishPageUpdated", { action: "send", channel: "page.updated" });',
  'events.toJson().asyncapi; // "3.0.0"</code></pre>',

  '<h3>b.asyncapi.parse(input, opts?) <a class="anchor" href="#asyncapi-parse">#</a></h3>',
  '<p>Parses and validates a minimum AsyncAPI document from JSON text or object input.</p>',
  '<pre><code class="language-javascript">var parsed = b.asyncapi.parse({',
  '  asyncapi: "3.0.0",',
  '  info: { title: "Wiki Events", version: "1.0.0" },',
  '  channels: {},',
  '  operations: {},',
  '});',
  'parsed.doc.info.version; // "1.0.0"</code></pre>',

  '<h2 id="asyncapi-yaml">b.asyncapi.toYaml(doc) <a class="anchor" href="#asyncapi-yaml">#</a></h2>',
  '<p>Serializes an AsyncAPI document into YAML for publishing to portals or partner-facing contract repositories.</p>',
  '<pre><code class="language-javascript">typeof b.asyncapi.toYaml; // "function"</code></pre>',

  '<h2 id="asyncapi-schema-walk">b.asyncapi.schemaWalk(schema) <a class="anchor" href="#asyncapi-schema-walk">#</a></h2>',
  '<p>Converts framework safe-schema definitions into AsyncAPI schema fragments.</p>',
  '<pre><code class="language-javascript">typeof b.asyncapi.schemaWalk; // "function"</code></pre>',

  '<h2 id="asyncapi-security">b.asyncapi.security(options) <a class="anchor" href="#asyncapi-security">#</a></h2>',
  '<p>Builds reusable AsyncAPI security scheme fragments for documented event transports.</p>',
  '<pre><code class="language-javascript">typeof b.asyncapi.security; // "function"</code></pre>',

  '<h2 id="asyncapi-bindings">b.asyncapi.bindings(options) <a class="anchor" href="#asyncapi-bindings">#</a></h2>',
  '<p>Builds protocol binding fragments for Kafka, MQTT, AMQP, HTTP, and WebSocket event contracts.</p>',
  '<pre><code class="language-javascript">typeof b.asyncapi.bindings; // "function"</code></pre>',

  '<h2 id="asyncapi-traits">b.asyncapi.traits(options) <a class="anchor" href="#asyncapi-traits">#</a></h2>',
  '<p>Builds reusable AsyncAPI operation and message traits shared across event channels.</p>',
  '<pre><code class="language-javascript">typeof b.asyncapi.traits; // "function"</code></pre>',

  '<h2 id="cloud-events-parse">b.cloudEvents.parse(envelope) <a class="anchor" href="#cloud-events-parse">#</a></h2>',
  '<p>Validates an inbound CloudEvents 1.0 envelope and surfaces extension attributes separately so consumers can route on operator-defined fields without grepping the envelope. Throws <code>CloudEventsError</code> with a <code>cloud-events/&lt;reason&gt;</code> code on missing required attributes (id / source / specversion / type), unsupported specversion, simultaneous <code>data</code> + <code>data_base64</code> (CloudEvents §3.1.1), or malformed extension names. Buffer-typed binary payloads encoded as <code>data_base64</code> are decoded back to a Buffer on <code>data</code>.</p>',
  '<pre><code class="language-javascript">var parsed = b.cloudEvents.parse({',
  '  specversion: "1.0",',
  '  id:          "evt-1",',
  '  source:      "/services/orders",',
  '  type:        "com.example.order.created",',
  '  data:        { id: "o-1234" },',
  '});',
  'parsed.type;           // "com.example.order.created"',
  'parsed.extensions;     // {} — operator-defined attrs surfaced here',
  'parsed.data.id;        // "o-1234"</code></pre>',

  '<h2 id="pagination">Pagination</h2>',
  '<h3>b.pagination.encodeCursor(state, secret) <a class="anchor" href="#pagination-encode-cursor">#</a></h3>',
  '<p>Encodes a cursor state object into the framework\'s tagged <code>&lt;base64url state&gt;.&lt;base64url tag&gt;</code> token. The state is canonicalized via RFC 8785 JCS, prefixed with a version field, and tagged with a SHA3-512 HMAC truncated to 128 bits. <code>secret</code> may be a Buffer or a non-empty string; an empty secret throws <code>PaginationError</code>. Use this when building cursors from raw SQL or row sources that don\'t fit <code>b.pagination.cursor</code>.</p>',
  '<pre><code class="language-javascript">var token = b.pagination.encodeCursor(',
  '  { orderKey: ["createdAt:asc", "_id:asc"], vals: [1700000000, "abc"], forward: true },',
  '  "wiki-validator-cursor-secret-32-chars-pad"',
  ');',
  'token.indexOf(".") > 0; // true</code></pre>',

  '<h3>b.pagination.decodeCursor(token, secret) <a class="anchor" href="#pagination-decode-cursor">#</a></h3>',
  '<p>Decodes and verifies a cursor token produced by <code>encodeCursor</code>. Verifies the SHA3-512 HMAC tag in constant time, refuses tampered or wrong-secret tokens, refuses tokens whose state JSON exceeds 8 KiB, and refuses cursor versions newer than the framework supports. Returns the decoded state object so the caller can build their own keyset WHERE clause.</p>',
  '<pre><code class="language-javascript">var token = b.pagination.encodeCursor({ pos: 42 }, "wiki-validator-cursor-secret-32-chars-pad");',
  'var state = b.pagination.decodeCursor(token, "wiki-validator-cursor-secret-32-chars-pad");',
  'state.pos;  // 42',
  'state.v;    // 1 — cursor version</code></pre>',

  '<h3>b.pagination.offset(query, opts) <a class="anchor" href="#pagination-offset">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  page:      number,    // default: 1; coerced to 1 when missing or invalid',
  '  perPage:   number,    // default: opts.default || 25; capped at opts.max',
  '  max:       number,    // default: 100',
  '  default:   number,    // default: 25',
  '  orderBy:   string,    // default: "_id"; identifier-validated against safeSql',
  '  direction: string,    // default: "asc" | "desc"',
  '}</code></pre>',
  '<p>Page-numbered offset pagination for legacy clients that render numbered nav. Returns <code>{ items, total, page, perPage, totalPages, hasMore }</code>. Use cursor pagination instead when depth is high — offset at depth scans-and-skips O(n) rows and concurrent writes shift the offset, so rows are missed or duplicated. Identifier validation on <code>orderBy</code> blocks SQL injection at the call site.</p>',
  '<pre><code class="language-javascript">// Live use:',
  '// var page = await b.pagination.offset(b.db.from("users"), {',
  '//   page: req.query.page, perPage: req.query.perPage, max: 100, default: 25,',
  '// });',
  'typeof b.pagination.offset; // "function"</code></pre>',

  '<h2 id="sse">Server-Sent Events</h2>',
  '<h3>b.sse.serializeEvent(opts) <a class="anchor" href="#sse-serialize">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  event: string,     // optional event name; LF/CR/NUL refused',
  '  id:    string,     // optional event id; LF/CR/NUL refused',
  '  data:  string,     // optional data; LF split into multiple data lines',
  '  retry: number,     // optional non-negative integer reconnect advisory',
  '}</code></pre>',
  '<p>Serializes one SSE frame with newline-injection refusal for fields that would otherwise let an attacker forge extra event-stream lines.</p>',
  '<pre><code class="language-javascript">var frame = b.sse.serializeEvent({',
  '  id: "42",',
  '  event: "page.updated",',
  '  data: "{\\"slug\\":\\"welcome\\"}",',
  '});',
  'frame.indexOf("event: page.updated") >= 0; // true</code></pre>',

  '<h3>b.sse.create(req, res, opts) <a class="anchor" href="#sse-create">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  heartbeatMs: number,      // default: 15 seconds; 0 disables keepalive',
  '  retryMs:     number,      // optional opening retry field',
  '  errorClass:  errorClass,  // default: SseError',
  '  audit:       boolean,     // default: true',
  '}</code></pre>',
  '<p>Wires an HTTP response as <code>text/event-stream</code> and returns a channel with <code>send</code>, <code>comment</code>, <code>close</code>, and sanitized <code>lastEventId</code>.</p>',
  '<pre><code class="language-javascript">function stream(req, res) {',
  '  var channel = b.sse.create(req, res, { retryMs: 5000 });',
  '  channel.send({ event: "ready", data: "ok" });',
  '  return channel;',
  '}</code></pre>',

  '<h2 id="websocket-client">Outbound WebSocket Client</h2>',
  '<h3>b.wsClient.connect(url, opts?) <a class="anchor" href="#ws-client-connect">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  subprotocols:       array,    // optional Sec-WebSocket-Protocol values',
  '  headers:            object,   // optional request headers',
  '  reconnect:          object,   // optional { maxAttempts, baseMs, maxMs }',
  '  pingMs:             number,   // default: 30 seconds',
  '  pongMs:             number,   // default: 60 seconds',
  '  maxMessageBytes:    number,   // default: 8 MiB',
  '  maxFrameBytes:      number,   // default: 8 MiB',
  '  handshakeTimeoutMs: number,   // default: 15 seconds',
  '  tlsOpts:            object,   // optional TLS options forwarded to the connect socket',
  '  tlsOptsFor:         function, // optional per-attempt TLS opts override',
  '  urlFor:             function, // optional per-attempt URL override',
  '  handshakeGuid:      string,   // optional non-default Sec-WebSocket-Key GUID',
  '}</code></pre>',
  '<p>Dials an outbound RFC 6455 WebSocket endpoint with handshake validation, client-side masking, heartbeat, reconnect backoff, and TLS verification on by default.</p>',
  '<pre><code class="language-javascript">var clientOptions = {',
  '  subprotocols: ["json-stream-v1"],',
  '  reconnect: { maxAttempts: 3, baseMs: 250, maxMs: 2000 },',
  '};',
  '// var client = b.wsClient.connect("wss://stream.example.com/v1", clientOptions);',
  'clientOptions.reconnect.maxAttempts; // 3</code></pre>',

  '<h2 id="mcp">Model Context Protocol</h2>',
  '<h3>b.mcp.parseRequest(body, opts?) <a class="anchor" href="#mcp-parse">#</a></h3>',
  '<p>Validates the JSON-RPC 2.0 envelope before an MCP tool or resource dispatcher sees it.</p>',
  '<pre><code class="language-javascript">var req = b.mcp.parseRequest({',
  '  jsonrpc: "2.0",',
  '  method: "tools/list",',
  '  id: 1,',
  '});',
  'req.method; // "tools/list"</code></pre>',

  '<h3>b.mcp.serverGuard(opts) <a class="anchor" href="#mcp-guard">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  requireBearer:           boolean,   // default: true',
  '  verifyBearer:            function,  // required when requireBearer=true',
  '  redirectUriAllowlist:    array,     // exact-match OAuth redirect URIs',
  '  allowDynamicRegister:    boolean,   // default: false',
  '  registerClientAllowlist: function,  // required when dynamic registration is enabled',
  '  toolAllowlist:           array,     // optional allowed tool names',
  '  resourceAllowlist:       array,     // optional allowed resource names',
  '  maxBodyBytes:            number,    // default: 1 MiB',
  '  errorClass:              errorClass,',
  '  audit:                   boolean,   // default: true',
  '}</code></pre>',
  '<p>Hardens MCP HTTP endpoints against unauthenticated tool calls, unsafe OAuth redirects, dynamic-client-registration abuse, and tool/resource overexposure.</p>',
  '<pre><code class="language-javascript">var guard = b.mcp.serverGuard({',
  '  verifyBearer: async function (token) {',
  '    return token === "test-token" ? { sub: "operator" } : null;',
  '  },',
  '  toolAllowlist: ["pages.search"],',
  '});',
  'typeof guard; // "function"</code></pre>',

  '<h3>b.mcp.refuse(res, code, message, id) <a class="anchor" href="#mcp-refuse">#</a></h3>',
  '<p>Writes a JSON-RPC error response with the framework\'s HTTP status mapping for parse, invalid-request, missing-method, and internal-error codes.</p>',
  '<pre><code class="language-javascript">var body = "";',
  'var res = { setHeader: function () {}, end: function (s) { body = s; } };',
  'b.mcp.refuse(res, -32601, "method not found", 7);',
  'res.statusCode; // 404</code></pre>',

  '<h2 id="graphql-federation">GraphQL Federation</h2>',
  '<h3>b.graphqlFederation.queryProbesSdl(query) <a class="anchor" href="#graphql-query-probes">#</a></h3>',
  '<p>Detects Apollo Federation <code>_service { sdl }</code> probes so the route can apply its federation trust boundary before returning schema SDL.</p>',
  '<pre><code class="language-javascript">b.graphqlFederation.queryProbesSdl("{ _service { sdl } }");',
  '// true</code></pre>',

  '<h3>b.graphqlFederation.guardSdl(opts) <a class="anchor" href="#graphql-guard">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  routerToken:     string,    // shared router bearer token',
  '  nonceStore:      object,    // optional replay store',
  '  maxQueryBytes:   number,    // optional query cap',
  '  audit:           boolean,   // default: true',
  '}</code></pre>',
  '<p>Protects the federation SDL route with a router-only bearer token and optional nonce replay defense.</p>',
  '<pre><code class="language-javascript">var guard = b.graphqlFederation.guardSdl({',
  '  routerToken: "router-local-test-token-32-chars-ok",',
  '});',
  'typeof guard; // "function"</code></pre>',

  '<h2 id="a2a">Agent-to-Agent Cards</h2>',
  '<h3>b.a2a.createCard(opts) <a class="anchor" href="#a2a-create-card">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  issuer:       string,  // required issuer id',
  '  agentId:      string,  // required agent id',
  '  capabilities: array,   // required capability names',
  '  endpoints:    array,   // optional HTTPS or localhost endpoints',
  '  policies:     object,  // optional policy metadata',
  '  contact:      object,  // optional operator contact',
  '  version:      string,  // default: "1.0.0"',
  '  metadata:     object,  // optional metadata',
  '}</code></pre>',
  '<p>Builds the validated A2A agent card another agent fetches before collaboration.</p>',
  '<pre><code class="language-javascript">var card = b.a2a.createCard({',
  '  issuer: "wiki.example",',
  '  agentId: "docs-agent",',
  '  capabilities: ["search"],',
  '  endpoints: [{ url: "https://wiki.example/a2a" }],',
  '});',
  'card.version; // "1.0.0"</code></pre>',

  '<h3>b.a2a.canonicalize(card) <a class="anchor" href="#a2a-canonicalize">#</a></h3>',
  '<p>Returns RFC 8785-aligned canonical JSON for signing and storage.</p>',
  '<pre><code class="language-javascript">var canonical = b.a2a.canonicalize({ b: 2, a: 1 });',
  'canonical; // "{\\"a\\":1,\\"b\\":2}"</code></pre>',

  '<h3>b.a2a.signCard(card, privateKeyPem, opts?) / b.a2a.verifyCard(envelope, publicKeyPem, opts?) <a class="anchor" href="#a2a-sign-verify">#</a></h3>',
  '<pre><code class="language-javascript">{',
  '  ttlMs:          number,    // signCard default: 24 hours',
  '  maxBytes:       number,    // verifyCard default: 64 KiB',
  '  clockSkewMs:    number,    // verifyCard default: 5 minutes',
  '  expectedIssuer: string,    // verifyCard optional issuer pin',
  '  audit:          boolean,   // default: true',
  '  errorClass:     errorClass,',
  '}</code></pre>',
  '<p>Signs an agent-card envelope and verifies signature, expiry, issuer pin, and required card fields.</p>',
  '<pre><code class="language-javascript">var unsignedCard = b.a2a.createCard({',
  '  issuer: "wiki.example",',
  '  agentId: "docs-agent",',
  '  capabilities: ["search"],',
  '});',
  '// var envelope = b.a2a.signCard(unsignedCard, privateKeyPem, { ttlMs: b.constants.TIME.hours(1) });',
  '// var verdict = b.a2a.verifyCard(envelope, publicKeyPem, { expectedIssuer: "wiki.example" });',
  'unsignedCard.agentId; // "docs-agent"</code></pre>',
  ].join("\n"),
};
