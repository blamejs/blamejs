{
  "$schema": "./release-note.schema.json",
  "version": "0.20.16",
  "date": "2026-09-15",
  "headline": "The CSRF and fetch-metadata method exemptions normalize case, and expiry checks fail closed on a non-numeric timestamp",
  "summary": "Two request-handling guards accepted malformed or non-canonical input instead of rejecting it. The CSRF and fetch-metadata middleware exempt safe methods by comparing the request method against an upper-cased list without normalizing the request method's case; over HTTP/2, which delivers the method verbatim, a request using a non-canonical case (such as `Post`) skipped the gate. Separately, the session, API-key, break-glass, and cluster leader-lease expiry checks used a NaN-blind numeric comparison, so a non-finite stored value (a missing or non-numeric timestamp from a custom store) read as never-expiring. Both are fixed to normalize and to fail closed.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "CSRF and fetch-metadata method exemptions normalize the request method case",
          "body": "`b.middleware.csrfProtect` and `b.middleware.fetchMetadata` exempt safe methods from their checks by testing the request method against the configured protected-method list. The list is upper-cased at construction, but the request method was compared without normalizing its case. HTTP/1.1 rejects a non-canonical method case before the handler runs, but HTTP/2 delivers the method verbatim, so a state-changing request using a non-canonical case (for example `Post`) fell outside the protected list and skipped the gate, including the Origin and token / Sec-Fetch checks. The request method is now upper-cased before the exemption test, so a protected method is gated whatever its case; safe methods remain exempt."
        },
        {
          "title": "Session, API-key, break-glass, and cluster-lease expiry checks fail closed on a non-finite timestamp",
          "body": "The session, API-key, break-glass grant, and cluster leader-lease expiry checks compared the stored expiry with `Number(value) < now`. When `Number(value)` is `NaN` (a missing key, or a non-numeric value such as an ISO date string returned by a custom store), the comparison is false, so the record read as not expired. A SQL NULL was already handled (`Number(null)` is 0), so the shipped stores were unaffected, but a custom `b.session.useStore` adapter or store returning a non-numeric expiry disabled expiry enforcement. These checks now treat a non-finite timestamp as already past, so a malformed expiry fails closed."
        }
      ]
    }
  ]
}
