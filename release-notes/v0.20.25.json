{
  "$schema": "./release-note.schema.json",
  "version": "0.20.25",
  "date": "2026-09-16",
  "headline": "Verifiable-credential verification checks revocation status and refuses a revocable credential it cannot check",
  "summary": "`b.auth.sdJwtVc.verify` and `b.auth.oid4vp.verifier.verifyResponse` verified a credential's signature, key binding, and validity window but never consulted its `status` claim (Token Status List), so a revoked credential verified as valid. Verification now checks status: when a credential carries a `status` claim it is revocable, and the verifier requires a `statusListResolver` to resolve its entry. A resolver that reports any non-valid status (revoked or suspended) rejects the credential; a credential that carries a `status` claim with no resolver configured is refused rather than accepted unchecked.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        {
          "title": "statusListResolver on the SD-JWT-VC and OpenID4VP verifiers",
          "body": "`b.auth.sdJwtVc.verify` accepts `opts.statusListResolver`, and `b.auth.oid4vp.verifier.create` accepts it as a create-time option threaded through to the credentials it verifies. The resolver is called as `statusListResolver({ idx, uri })` with the credential's Token Status List reference and returns the integer status for that index (0 = valid). Operators resolve the reference with `b.auth.statusList.fromJwt` (fetch the status-list token from `uri`, verify its signature under their trust configuration, and return `list.get(idx)`)."
        }
      ]
    },
    {
      "heading": "Security",
      "items": [
        {
          "title": "A revoked credential no longer verifies, and a revocable credential is refused when its status cannot be checked",
          "body": "`b.auth.sdJwtVc.verify` (and `b.auth.oid4vp.verifier.verifyResponse`, which composes it) checked signature, key binding, and the validity window but ignored the credential's `status` claim, so a credential the issuer had revoked still verified as valid. A credential carrying a `status` claim (Token Status List) is now treated as revocable: the verifier resolves the entry through `opts.statusListResolver` and rejects the credential (`auth-sd-jwt-vc/revoked`) when the resolved status is anything other than valid. A credential that carries a `status` claim while no `statusListResolver` is configured is refused (`auth-sd-jwt-vc/status-check-required`) rather than accepted without a revocation check. A credential with no `status` claim is unaffected."
        }
      ]
    },
    {
      "heading": "Migration",
      "items": [
        {
          "title": "Supply a statusListResolver to verify revocable credentials",
          "body": "If you verify credentials that carry a `status` claim through `b.auth.sdJwtVc.verify` or `b.auth.oid4vp.verifier`, supply `statusListResolver({ idx, uri })` returning the integer status for the credential's index (0 = valid). A typical resolver fetches the status-list token from `uri`, verifies it with `b.auth.statusList.fromJwt`, and returns `result.list.get(idx)`. Without a resolver, a credential carrying a `status` claim is refused with `auth-sd-jwt-vc/status-check-required`; a credential with no `status` claim continues to verify unchanged."
        }
      ]
    }
  ]
}
